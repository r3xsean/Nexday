<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:state_enabled="false" android:color="?attr/colorOnSurface" android:alpha="0.38" />
    
    <!-- Pressed state -->
    <item android:state_pressed="true" android:color="?attr/colorOnPrimary" />
    
    <!-- Focused state -->
    <item android:state_focused="true" android:color="?attr/colorOnPrimary" />
    
    <!-- Default state -->
    <item android:color="?attr/colorOnPrimary" />
</selector>